{
  "id": "next_frame",
  "name": "Predict next frame",
  "description": "Create the next frame of an animation from the current frame, preserving style and palette.",
  "category": "animation",
  "model": "image",
  "params_schema": {
    "type": "object",
    "properties": {
      "action": { "type": "string", "description": "What happens next" }
    }
  }
}
