[package]
name = "bixel-tui"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Terminal (TUI) chatbot harness for Bixel Studio: chat with the AI and run pixel-art skills via slash commands."

[[bin]]
name = "bixel"
path = "src/main.rs"

[dependencies]
bixel-ai = { path = "../bixel-ai" }
ratatui = "0.29"
crossterm = "0.28"
serde_json = { workspace = true }
